---

- hosts: localhost
  gather_facts: yes

  tasks:
    - name: check version of requests
      shell: hostname

    - name: Download foo.conf
      get_url:
       url: https://bootstrap.pypa.io/get-pip.py
       dest: /tmp/get-pip.py

    - name: install pip3
      sudo: yes
      shell: python3 /tmp/get-pip.py

    - name: install pip3
      shell: python3 ver.py
      register: requests_pip
    
    - debug: var=requests_pip.stdout
   
